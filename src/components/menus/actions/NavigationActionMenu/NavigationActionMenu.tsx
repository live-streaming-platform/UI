import React from 'react';

type NavigationActionMenuPropsType = {
    type: "notifications" | "userdata";

}

const NavigationActionMenu = (props: NavigationActionMenuPropsType) => {
    return (
        <div>

        </div>
    );
};

export default NavigationActionMenu;